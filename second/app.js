const app = Vue.createApp({
    data() {
        return {
            courseGoal: 'Finish the course on Time!',
            vueLink: 'https://vuejs.org'
        };
    }
});


app.mount('#user-goal');